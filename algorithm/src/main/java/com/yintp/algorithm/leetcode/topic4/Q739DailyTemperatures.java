package com.yintp.algorithm.leetcode.topic4;

/**
 * #739 每日温度 (Daily Temperatures) - Medium
 * 专题: 单调栈 | 第6周 Day3
 *
 * 给定一个整数数组 temperatures，返回数组 answer，
 * answer[i] 是指对于第 i 天，下一个更高温度出现在几天后。
 * 如果气温在这之后都不会升高，answer[i] 为 0。
 *
 * 示例: [73,74,75,71,69,72,76,73] → [1,1,4,2,1,1,0,0]
 *
 * 思路: 单调递减栈(存下标) - 当前温度比栈顶大时，弹出并计算距离
 */
public class Q739DailyTemperatures {
    public int[] dailyTemperatures(int[] temperatures) {
        // TODO: 单调递减栈
        return new int[0];
    }
}
